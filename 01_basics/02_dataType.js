"use strict" // this will treat js code as newer version
// alert(2+3) // error will show as we are using nodejs , not browser

let name="RItesh" //String u can use in ' '.
let age =3 //Number it no is large u can use bigint
let isLogin=false //boolean
null // standalone value-> it is not zero it is no value available
undefined // value not defined
Symbol // primary purpose is to have a unique result every time they are constructed.
// typeof -> It will give u the datatype
console.log(typeof null) //op -object(null is a object)
console.log(typeof undefined) //op - undefined 
console.log(typeof Symbol) // op- functiom

//console.table([name,age,isLogin,null,undefined])